export type Departure = {
    arrival: string
    operator: string
    ship: string
    time: string
    passengers: number
    vehicle: number
}

export type Seats = {
    code: string
    name: string
}
